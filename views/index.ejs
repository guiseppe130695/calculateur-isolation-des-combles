<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🏠 Calculateur Isolation des Combles</title>
    <link rel="stylesheet" href="/styles/main.css">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>🏠 Calculez votre projet d'isolation des combles</h1>
        
        <div class="progress-bar">
            <div class="progress-bar-inner"></div>
        </div>
        <div class="progress-text"></div>

        <form onsubmit="handleFormSubmit(event)">
            <% formData.steps.forEach((step, stepIndex) => { %>
                <div class="step">
                    <h2 class="step-title"><%= step.title %></h2>
                    <div class="step-content">
                        <% step.sections.forEach(section => { %>
                            <div class="form-group">
                                <h3 class="section-title"><%= section.title %></h3>
                                <div class="option-grid">
                                    <% section.options.forEach(option => { %>
                                        <label class="card-option">
                                            <input 
                                                type="<%= section.type %>" 
                                                name="<%= section.id %><%= section.type === 'checkbox' ? '[]' : '' %>" 
                                                value="<%= option.value %>"
                                                class="<%= section.id %>-<%= section.type %>">
                                            <div class="card-content">
                                                <div class="card-icon"><%= option.icon %></div>
                                                <div class="card-label"><%= option.label %></div>
                                            </div>
                                        </label>
                                    <% }); %>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                </div>
            <% }); %>

            <div class="navigation-buttons">
                <button type="button" class="prev-button" onclick="prevStep()">← Précédent</button>
                <button type="button" class="next-button" onclick="nextStep()">Suivant →</button>
                <button type="submit" class="submit-button">Calculer mon projet 🚀</button>
            </div>
        </form>
    </div>
    <script src="/js/formHandler.js"></script>
</body>
</html>